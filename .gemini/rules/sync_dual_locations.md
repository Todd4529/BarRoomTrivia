# Dual Project Directory Synchronization Rule

Whenever modifying, adding, or deleting files in `e:\Coding\BarRoomTrivia` (including source code in `lib/`, web code in `src/` and `index.html`, assets in `assets/`, tests in `test/`, or configuration files):

- Automatically copy/synchronize those changes to `C:\Coding\BarRoomTrivia` before completing the task.
- Ensure both `e:\Coding\BarRoomTrivia` and `C:\Coding\BarRoomTrivia` remain 100% in sync.
